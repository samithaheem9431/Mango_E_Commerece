{
  "name": "mango-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "both": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
